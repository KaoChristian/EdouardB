<form method="post" action="{{ path('admin_section_categories_delete', {'id': section_category.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ceci ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ section_category.id) }}">
    <button class="btn btn-secondary">
      <i class="fas fa-trash"></i>
    </button>
</form>
