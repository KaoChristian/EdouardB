<form method="post" action="{{ path('app_sections_delete', {'id': section.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ceci ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ section.id) }}">
    <button class="btn btn-secondary">
        <i class="fas fa-trash"></i>
    </button>
</form>
