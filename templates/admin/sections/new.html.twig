{% extends 'admin/base.html.twig' %}

{% block title %}Créer une section{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-body">
            <h2 class="text-center mb-4">Créer une section</h2>

            {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn">{{ button_label|default('Save') }}</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
