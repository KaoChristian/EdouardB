{% extends 'base.html.twig' %}

{% block title %}SectionCategory index{% endblock %}

{% block body %}
    <h1>SectionCategory index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for section_category in section_categories %}
            <tr>
                <td>{{ section_category.id }}</td>
                <td>{{ section_category.name }}</td>
                <td>
                    <a href="{{ path('app_section_category_show', {'id': section_category.id}) }}">show</a>
                    <a href="{{ path('app_section_category_edit', {'id': section_category.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_section_category_new') }}">Create new</a>
{% endblock %}
