{% extends 'base.html.twig' %}

{% block title %}SectionCategory{% endblock %}

{% block body %}
    <h1>SectionCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ section_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ section_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_section_category_index') }}">back to list</a>

    <a href="{{ path('app_section_category_edit', {'id': section_category.id}) }}">edit</a>

    {{ include('section_category/_delete_form.html.twig') }}
{% endblock %}
